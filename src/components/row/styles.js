import {StyleSheet} from 'react-native';

const styles = props => {
  return StyleSheet.create({
    row: {
      display: 'flex',
      flexWrap: 'nowrap',
      flexDirection: 'row',
      maxWidth: '100%',
    },
  });
};
export default styles;
